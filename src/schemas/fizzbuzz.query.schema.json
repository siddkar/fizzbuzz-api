{
    "id": "query",
    "type": "object",
    "properties": {
        "count": {
            "id": "#/properties/count",
            "type": "string",
            "pattern": "^[1-9][0-9]*$",
            "errorMessage": "should be an integer with value greater than 0"
        }
    }
}
